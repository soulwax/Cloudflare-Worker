{
	"name": "neuro-explorer",
	"version": "0.15.0",
	"type": "module",
	"private": true,
	"scripts": {
		"build": "npm run typecheck",
		"typecheck": "tsc --noEmit",
		"build:web": "npm --prefix web run build",
		"cf-typegen:web": "npm --prefix web run cf-typegen",
		"deploy": "wrangler deploy",
		"deploy:web": "npm --prefix web run deploy",
		"deploy:vercel": "npx vercel --prod",
		"dev": "wrangler dev",
		"dev:web": "npm --prefix web run dev",
		"preview:web": "npm --prefix web run preview:cf",
		"dev:vercel": "npx vercel dev",
		"start": "wrangler dev",
		"test": "vitest",
		"typecheck:web": "npm --prefix web run typecheck",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"@types/node": "^25.3.3",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.71.0"
	},
	"dependencies": {
		"liquidjs": "^10.24.0",
		"markdownlint": "^0.40.0"
	}
}
